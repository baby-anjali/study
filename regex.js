// 1. Write a regular expression to validate an email address.
const emailValidate = /\w+@g?mail.com/;

// 2. Create a regex pattern to match a U.S. phone number format:
// `(123) 456-7890`.
const usPhoneValidate = /\(\d{3}\) \d{3}-\d{4}/;
